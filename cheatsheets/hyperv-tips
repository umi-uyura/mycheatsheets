---
tags: [windows]
---

#
# Hyper-V tips
#

# CLI operation
# - Need administrator privileges
# - If running from the command prompt, use `powershell -Command "<command>"`

Get-VM
Start-VM <vm name>
Stop-VM <vm name>

#
# Nat
#

Get-NetNat
New-NetNat -Name <nat name> -InternalIPInterfaceAddressPrefix <ip range>
Remove-NetNat -Name <nat name>

# Create NAT network
# - ifIndex ... Obtain by `Get-NetAdapter` command
New-NetIPAddress -IPAddress <ip address> -PrefixLength <netmask> -InterfaceIndex <ifIndex>
