---
tags: [macos, linux]
---

#
# brew - Homebrew (macOS/Linux)
#

brew install <package>
brew show <package>
brew uninstall <package>

# Find packages
brew search <keyword>

# Fetch the newest version of Homebrew and all formulae
brew update

# Display outdated package (Run after `brew update`)
brew outdated

# Upgrade packages (Run after `brew update`)
brew upgrade
brew upgrade <package>


#
# Homebrwe cask support (macOS only)
#

brew cask outdated
brew upgrade --cask
