---
tags: [linux, tips]
---

#
# Linux tips
#

# Performance survey command
uptime          # load average
w               # load average, login user
top             # load average, cpu usage, memory usage
free            # memory usage
vmstat          # statistics including cpu, memory, disk i/o
sar             # package: sysstat
dstat           # package: dstat

# Storage
df              # Disk usage (system)
du              # Disk usage (directory)
iostat          # package: sysstat

# Network
ss              # investigate sockets
netstat         # investigate sockets, package: net-tools
traceroute      # package trace, package: traceroute
nc              # Send/listens TCP and UDP
nslookup        # query DNS
dig             # DNS lookup

# Show os /  kernel information
uname -a
uname -r
cat /etc/os-release
cat /etc/lsb-release
cat /proc/version
lsb_release -a
lsb_release -r
hostnamectl

# Show hardware information
lscpu                   # CPU
cat /proc/cpuinfo       # CPU -> `lscpu`
cat /proc/meminfo       # Memory -> `free`
lshw                    # Hardware
lsblk                   # Brock devices
